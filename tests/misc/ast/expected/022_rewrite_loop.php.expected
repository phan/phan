<?php

while ($x) { if (!$y) { break; }
    var_export($x);
    var_export($y);
}

for ($i = 0; true, $i < 2; $i++) { if (!is_string($x)) { break; }
    var_export([$i, $x]);
}

while ($x) {
}

for (;$i=0,$x;) {
}
